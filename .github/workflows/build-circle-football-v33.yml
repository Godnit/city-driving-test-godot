name: Build Circle Football v3.3

on:
  push:
    branches: [circle-football-v3.3-v22-gameplay-modern-visuals]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Build v3.3 APK
        shell: bash
        run: bash circle_football_native/build_v33.sh
      - uses: actions/upload-artifact@v4
        with:
          name: CircleFootball-Android81-v3.3-APK
          path: circle_football_native/build/CircleFootball-Android81-v3.3.apk
          if-no-files-found: error
          retention-days: 14
      - uses: actions/upload-artifact@v4
        with:
          name: CircleFootball-Android81-v3.3-Report
          path: |
            circle_football_native/build/badging.txt
            circle_football_native/build/size.txt
            circle_football_native/build/apk-files.txt
          if-no-files-found: error
          retention-days: 7
