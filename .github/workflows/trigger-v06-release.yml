name: Trigger v0.6 Release Build

on:
  issues:
    types: [opened]

permissions:
  actions: write
  issues: write
  contents: read

jobs:
  dispatch:
    if: ${{ github.event.issue.title == 'run-v0.6-release' || github.event.issue.title == 'run-v0.6-branch' }}
    runs-on: ubuntu-24.04
    steps:
      - name: Dispatch verified v0.6 branch build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          set -euo pipefail
          ref='main'
          if [ "$ISSUE_TITLE" = 'run-v0.6-branch' ]; then
            ref='v0.6-driving-rebuild'
          fi
          curl --fail-with-body -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/$REPO/actions/workflows/build-android.yml/dispatches" \
            -d "{\"ref\":\"$ref\"}"

      - name: Close trigger issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_URL: ${{ github.event.issue.url }}
        run: |
          curl --fail-with-body -L \
            -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$ISSUE_URL" \
            -d '{"state":"closed"}'
